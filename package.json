{
  "name": "rdf-web-components-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/larsw/rdf-web-components"
  },
  "author": {
    "name": "larsw",
    "email": "lars@lars-backwards.org",
    "url": "https://github.com/larsw"
  },
  "license": "MIT",
  "scripts": {
    "build": "bun run --filter @sral/rdf-web-components build && bun run --filter @sral/react-rdf-components build",
    "dev": "bun run --filter @sral/rdf-web-components dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "prettier --check \"**/*.{ts,tsx,md}\"",
    "test": "bun run --filter @sral/rdf-components-shared test && bun run --filter @sral/rdf-web-components test && bun run --filter @sral/react-rdf-components test"
  },
  "devDependencies": {
    "prettier": "3.6.2",
    "typescript": "5.9.2"
  }
}
